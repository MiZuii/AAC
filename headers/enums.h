#ifndef ENUMS_H
#define ENUMS_H

enum class AAC_error_codes {
  INVALID_PIXEL,
  INVALID_PATH,
};

enum class AAC_Pixel_Type {
    EMPTY,
    G,
    GA,
    RGB,
    RGBA,
};

#endif // ENUMS_H