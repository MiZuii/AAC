#include <aac.h>
#include <ttf_reader.h>

/**
 * @file aac_cc_simplef.cpp
 * @brief Contains the implementation of the CC_SimpleF class.
 */

using namespace AAC;

/**
 * @brief Converts the given matrix of chunks to a string by matching chunks to font glyphs.
 *
 * @param chunks A pointer to the matrix of chunks to be converted.
 * @return The resulting string representation of the converted chunks.
 */
std::string CC_SimpleF::convert(Matrix<Chunk>* chunks) {
    
    return std::string("omg");
}